dependencyResolutionManagement {
    versionCatalogs {
        libs {
            from(files("../../config/libs.versions.toml"))
        }
    }
}

includeBuild '../core-logging'
includeBuild '../core-metrics'
includeBuild '../core-future'