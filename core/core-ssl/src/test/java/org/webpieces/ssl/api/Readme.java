package org.webpieces.ssl.api;

/**
 * All tests are actually in core-channelmanager2 OR core-asyncserver for SSL such that it makes it easier 
 * to refactor the ssl api (ie. we ported the old api tests so we could refactor the api and know that it
 * still worked)
 */
public class Readme {

}
