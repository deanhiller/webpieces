project(':embeddablewebserver') {
    dependencies {
        compile 'com.google.inject:guice:4.0'
        compile project(':http-frontend'), project(':http-parser1_1'), project(':http-router')
        compile project(':http-templating')
    }
}
