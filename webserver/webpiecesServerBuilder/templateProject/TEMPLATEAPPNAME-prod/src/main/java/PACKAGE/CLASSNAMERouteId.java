package PACKAGE;

import org.webpieces.router.api.routing.RouteId;

public enum CLASSNAMERouteId implements RouteId {
	
	//This is where you define the ids of routes that you can use in the controllers to redirect to a route
	//or use in the webpages
	REDIRECT_PAGE, RENDER_PAGE, SOME_ROUTE, ASYNC_ROUTE, ANOTHER, EXTRA_ROUTE, ABSOLUTE, ABSOLUTE2
}
