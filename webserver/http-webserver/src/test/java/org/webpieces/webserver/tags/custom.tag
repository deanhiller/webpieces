#{extends 'supertemplate.tag'/}#

This is a custom tag which can also use tags in itself #{a href:@VERBATIM[:]}#Some Link Here#{/a}#

<div>The user is ${user}$ and ${user2}$ tag argument ${tagarg}$</div>
