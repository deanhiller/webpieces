package org.webpieces.webserver.filters.app;

import org.webpieces.router.api.routes.RouteId;

public enum FiltersRouteId implements RouteId {
	HOME, LOGIN, POST_LOGIN, LOGIN_BACKEND, INTERNAL, HTTP_ROUTE, HTTPS_ROUTE, HTTPS_ROUTE2, HTTP_ROUTE2, BACKEND
	
}