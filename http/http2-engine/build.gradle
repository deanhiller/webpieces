dependencies {

    api project(':http:http2-hpack')
    api project(':core:core-statemachine')

}

publishing {
    publications {
        mavenJava(MavenPublication) {
            pom {
                description = 'A re-usable asynchronous HTTP/2 parser'
            }
        }
    }
}

