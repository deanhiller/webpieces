dependencies {
    compile deps['twitter-hpack']
	compile project(':core:core-util')
    compile project(':http:http2-parser')
}

uploadArchives.repositories.mavenDeployer.pom.project {
   description='A re-usable asynchronous http2 parser WITH header deserialization/serialization(hpack)'
}
