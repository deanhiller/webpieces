dependencies {
    api project(':http:http2-hpack')
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            pom {
                description = 'Translations from HTTP/1.1 to HTTP/2 and back'
            }
        }
    }
}
