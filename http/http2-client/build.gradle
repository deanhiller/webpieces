dependencies {
    compile project(':core:core-channelmanager2') 
    compile project(':http:http2-engine')
	
	testCompile project(':core:core-mock')
}

uploadArchives.repositories.mavenDeployer.pom.project {
   description='Strictly an http/2 client for when you know the server does http2 keeping it KISS'
}
