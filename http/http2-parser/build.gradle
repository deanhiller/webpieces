dependencies {

    api project(':core:core-datawrapper')
    api project(':http:http2-api')

    testImplementation project(':core:core-metrics')

    //testCompile project(':core:core-util')
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            pom {
                description = 'A re-usable asynchronous HTTP/2 parser'
            }
        }
    }
}
