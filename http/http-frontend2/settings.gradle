dependencyResolutionManagement {
    versionCatalogs {
        libs {
            from(files("../../config/libs.versions.toml"))
        }
    }
}

includeBuild '../../core/core-mock'
includeBuild '../../core/core-asyncserver'
includeBuild '../http2-translations'
