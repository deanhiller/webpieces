dependencyResolutionManagement {
    versionCatalogs {
        libs {
            from(files("../../config/libs.versions.toml"))
        }
    }
}

includeBuild '../http-frontend2'
includeBuild '../http2to1_1-client'
includeBuild '../../core/core-mock'
