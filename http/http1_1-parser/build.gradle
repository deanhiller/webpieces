dependencies {

    api project(':core:core-datawrapper')

}

publishing {
    publications {
        mavenJava(MavenPublication) {
            pom {
                description = 'A re-usable asynchronous HTTP/1.1 parser that can be used with any NIO client'
            }
        }
    }
}
