dependencyResolutionManagement {
    versionCatalogs {
        libs {
            from(files("../../config/libs.versions.toml"))
        }
    }
}


includeBuild '../../core/core-channelmanager2'
includeBuild '../../core/core-mock'
includeBuild '../http1_1-parser'
