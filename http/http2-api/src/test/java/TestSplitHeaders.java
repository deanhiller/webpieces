import org.junit.Assert;
import org.junit.Before;
import org.junit.Test;

/**
 * Nothing really to test as they are all just case-classes (ie. data objects with no business logic)
 * AND they should be kept that way to keep them simple and usale.
 * @author dean
 *
 */
public class TestSplitHeaders {


	@Before
	public void setup() {
	}
	
	@Test
	public void testBytesParsedBySplittingHeaders() {
		
	}
}
