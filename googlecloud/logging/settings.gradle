dependencyResolutionManagement {
    versionCatalogs {
        libs {
            from(files("../../config/libs.versions.toml"))
        }
    }
}

includeBuild '../../core/core-jackson'
includeBuild '../../core/core-future'
includeBuild '../../cloud/cloud-api-lib'
